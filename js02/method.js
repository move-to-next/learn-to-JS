// 원시객체의 특징
// 1. 불변성을 띄고 있어서 값이 변하지 않는다.
// 2. 값에 의한 전달을 받기때문에 다른변수에서 값을 복사할때 값만 새로운주소를 만들어서 거기에 복사되어 전달하는거지 주소까지 참조시키는게 아니다.
// 3. 0번째부터 시작한다.
// 4. 값과 타입이 모두 같아야 true가 된다.

// 문자열 메서드
// indexOf, subStr, includes, repeat, trim, match, replace, replaceAll, slice, split, toUpperCase, toLowerCase, subString, padStart

// 찾는메서드 - includes, match, replace, replaceAll, indexOf
// 바꾸는메서드 - toUpperCase, toLowerCase, trim, replace, replaceAll, padStart
// 자르는메서드 - slice, subString, subStr
// 분할해서 배열할당 - match, split
// 반복하는 메서드 - repeat